[pytest]
testpaths = .
python_files = test_*.py
norecursedirs = .venv bash __pycache__ .git
markers =
    integration: Tier 2 sandbox tests (GitHub + GCP). Required for release; see tests/run-integration-tests.sh
addopts = -ra -m "not integration"