# {{SERVICE_NAME}}

Streamlit app on the Shop Golden Path.

## Local

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy

Push to `main` → deploys to `dev`. Health: `/_stcore/health`.